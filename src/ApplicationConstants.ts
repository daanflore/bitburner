export const constants: ApplicationConstants = {
    TaskPort: 1,
    AccPort: 2,
    ResultPort: 3,
};

interface ApplicationConstants {
    TaskPort: number;
    AccPort: number;
    ResultPort: number;
} 